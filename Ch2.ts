/**
 * Logs the values of an object to the console.
 *
 * @param obj - The object to log.
 *
 * @example
 * ```ts
 * logValues({ a: 1, b: 2 });
 * // Output:
 * // a: 1
 * // b: 2
 * ```
 */

/**
 * Sums two numbers
 * 
 * @example
 * sum(1, 2)
 * // Result: 3
 */
const sum = (a: number, b: number) => {
  return a + b;
};